# Electron Support

Electron is the only supported framework in the current sesy release. The tool assumes your remote project exposes a renderer or dev URL that a local Electron `BrowserWindow` can load directly.
